# Runtime dependencies for the active sparse-view reconstruction pipeline.
# Python 3.10-3.12 is recommended.
#
# For RTX 4090D, install a CUDA-enabled PyTorch wheel that supports Ada
# (compute capability 8.9). The version range below is tested by this project
# with torch 2.6; choose the CUDA wheel/index appropriate for the host driver.

torch>=2.3,<2.7
numpy>=1.26,<3
scipy>=1.11,<2
scikit-image>=0.24,<0.27
Pillow>=10,<12
nibabel>=5.2,<6
SimpleITK>=2.3,<3
tensorboard>=2.15,<3
